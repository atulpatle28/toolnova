import { QrCodeGeneratorTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "QR Code Generator | ToolNova",
  description: "Generate QR codes for URLs, text and digital contact details in seconds.",
};

export default function Page() {
  return <QrCodeGeneratorTool />;
}
