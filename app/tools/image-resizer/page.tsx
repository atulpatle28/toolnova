import { ImageResizerTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "Image Resizer | ToolNova",
  description: "Resize images to exact pixels for social posts, web use and print-ready media.",
};

export default function Page() {
  return <ImageResizerTool />;
}
