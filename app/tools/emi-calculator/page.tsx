import { EmiCalculatorTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "EMI Calculator | ToolNova",
  description: "Estimate monthly EMI, total payable and interest with our loan calculator.",
};

export default function Page() {
  return <EmiCalculatorTool />;
}
