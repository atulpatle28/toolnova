import { SipCalculatorTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "SIP Calculator | ToolNova",
  description: "Estimate the future value of your SIP investments with monthly contribution planning.",
};

export default function Page() {
  return <SipCalculatorTool />;
}
