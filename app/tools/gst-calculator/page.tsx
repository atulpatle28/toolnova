import { GstCalculatorTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "GST Calculator | ToolNova",
  description: "Calculate GST, CGST, SGST and total amount with a fast and simple GST calculator.",
};

export default function Page() {
  return <GstCalculatorTool />;
}
