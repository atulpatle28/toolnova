import { PdfMergeTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "PDF Merge | ToolNova",
  description: "Merge multiple PDF files in your browser with drag-and-drop ordering and instant download.",
};

export default function Page() {
  return <PdfMergeTool />;
}
