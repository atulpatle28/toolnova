import { PercentageCalculatorTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "Percentage Calculator | ToolNova",
  description: "Calculate percentage values, discounts and share of a number instantly.",
};

export default function Page() {
  return <PercentageCalculatorTool />;
}
