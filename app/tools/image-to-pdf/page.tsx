import { ImageToPdfTool } from "@/app/components/ToolComponents";

export const metadata = {
  title: "Image to PDF | ToolNova",
  description: "Convert images into a downloadable PDF document for fast sharing and archiving.",
};

export default function Page() {
  return <ImageToPdfTool />;
}
